
const reemplazarEspacios = function(cadena) {
    return cadena.replace(/ /g, '-');
};

console.log(reemplazarEspacios("Reemplazar espacios por guiones"));
console.log(reemplazarEspacios("Crea una función que reemplace los espacios en una cadena por guiones"));
console.log(reemplazarEspacios(""));